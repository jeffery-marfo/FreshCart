# Leave unset for local dev — Vite's dev server proxies /api to the checkout-api
# (see vite.config.ts). Set this only for a production build where the storefront
# is served separately from the API (e.g. a static bucket, or its own container).
# VITE_API_BASE_URL=https://api.freshcart.example.com

{
  "name": "freshcart-storefront",
  "version": "1.0.0",
  "private": true,
  "description": "FreshCart storefront — a static site backed by the checkout API.",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vite": "^6.0.7"
  }
}
